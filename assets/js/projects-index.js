const root=document.querySelector('#projects');const filters=document.querySelector('#filters');const categories=['All',...new Set(window.PROJECTS.map(p=>p.category))];const draw=(category='All')=>{root.innerHTML=window.PROJECTS.filter(p=>category==='All'||p.category===category).map(p=>`<a class="archive-card" href="project.html?id=${p.id}"><span>${p.category} · ${p.year}</span><h2>${p.title}</h2><p>${p.summary}</p><b>Read case study ↗</b></a>`).join('')};filters.innerHTML=categories.map((c,i)=>`<button class="${i?'':'active'}" data-category="${c}">${c}</button>`).join('');filters.addEventListener('click',e=>{if(!e.target.matches('button'))return;filters.querySelectorAll('button').forEach(b=>b.classList.remove('active'));e.target.classList.add('active');draw(e.target.dataset.category)});draw();
